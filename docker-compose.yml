version: '3'

services:
  db:
    image: postgres:11.2-alpine

  web:
    build: .
    command: python /app/manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/app/
    ports:
      - 8000:8000
    depends_on:
      - db